# 修的媒体工作台 · 配置笔记

> 最后更新：V61 全功能版
> 前端地址：https://yxiu23.github.io/ai-workbench-dev/ai-workbench-dev.html
> 后端地址：https://ai-media-proxy.yxiu23.workers.dev

---

## 一、前端访问地址

| 项目 | 地址 |
|---|---|
| 自动跳转（推荐收藏） | https://yxiu23.github.io/ai-workbench-dev/ |
| 桌面端直连 | https://yxiu23.github.io/ai-workbench-dev/ai-workbench-dev.html |
| 移动端直连 | https://yxiu23.github.io/ai-workbench-dev/ai-workbench-mobile-dev.html |

---

## 二、速创 API 配置

| 项目 | 值 |
|---|---|
| API Key | `【你的速创API Key】` |
| API 基础地址 | `https://api.wuyinkeji.com` |
| 结果查询接口 | `GET /api/async/detail?id=<任务ID>` |
| 鉴权方式 | 请求头 `Authorization: <API Key>`（不带 Bearer 前缀） |
| 轮询超时 | 30 分钟 |
| QPS 限制 | 20 次/秒 |

---

## 三、R2 图床配置

| 项目 | 值 |
|---|---|
| Worker URL | `https://ai-media-proxy.yxiu23.workers.dev` |
| Auth Token | `【你的Auth Token】` |
| R2 Bucket 名 | `ai-media-storage` |
| R2 公开域名 | `pub-0f64bcd69f464228b22d20964f000919.r2.dev` |
| 上传接口 | `POST /upload?token=<Token>`（FormData 字段名 `file`） |
| 单文件上限 | 100MB |

⚠️ **注意**：`workers.dev` 域名在大陆可能间歇性不可达。如上传失败，建议绑定自定义域名。

---

## 四、任务中心配置（可选，推荐开启）

| 项目 | 值 |
|---|---|
| Worker URL | `https://ai-media-proxy.yxiu23.workers.dev` |
| Auth Token | `【你的Auth Token】` |
| KV Namespace | `TASKS`（ID: `【你的KV Namespace ID】`） |
| Cron 频率 | 每分钟（`* * * * *`） |
| 每日任务上限 | 200 次 |
| 并发限制 | 图片 2 / 视频 1 / 音频 2 |
| 自动重试 | 429/5xx/超时，最多2次，间隔60秒 |

**Worker 端 Secrets（已配置，勿删）：**
| Secret 名 | 值 |
|---|---|
| `SPEEDX_KEY` | `【你的速创API Key】` |
| `AUTH_TOKEN` | `【你的Auth Token】` |

**Worker 端环境变量：**
| 变量名 | 值 |
|---|---|
| `MAX_FILE_SIZE` | `104857600` |
| `PUBLIC_R2_DOMAIN` | `pub-0f64bcd69f464228b22d20964f000919.r2.dev` |
| `PUBLIC_BASE_URL` | `https://pub-0f64bcd69f464228b22d20964f000919.r2.dev` |
| `LIMIT_DAILY` | `200` |

---

## 五、工作台设置页填写顺序

打开 https://yxiu23.github.io/ai-workbench-dev/ai-workbench-dev.html → 右上角「设置」

### 1. API 配置
```
API Key:  【你的速创API Key】
```

### 2. R2 图床配置
```
Worker URL:  https://ai-media-proxy.yxiu23.workers.dev
Auth Token:  【你的Auth Token】
```

### 3. 任务中心（可选）
```
Worker URL:  https://ai-media-proxy.yxiu23.workers.dev
Auth Token:  【你的Auth Token】
```
> 注意：任务中心的 URL 和 Token 与 R2 配置相同（同一个 Worker）

---

## 六、Cloudflare 账号信息

| 项目 | 值 |
|---|---|
| Cloudflare 账号 | `Yxiu23@gmail.com` |
| Account ID | `【你的Cloudflare Account ID】` |
| Worker 名称 | `ai-media-proxy` |
| Worker 地址 | `https://ai-media-proxy.yxiu23.workers.dev` |
| R2 Bucket | `ai-media-storage` |
| KV Namespace | `TASKS` |

---

## 七、GitHub 仓库信息

| 项目 | 值 |
|---|---|
| 仓库地址 | https://github.com/yxiu23/ai-workbench-dev |
| 仓库克隆 | `https://github.com/yxiu23/ai-workbench-dev.git` |
| 部署方式 | git push → GitHub Pages 自动部署 |
| 生效时间 | push 后 1-2 分钟 |
| 分支 | `main` |

---

## 八、速创 API 接口清单

| 分类 | 产品 | 接口 | 价格 |
|---|---|---|---|
| 图片 | NanoBanana2 | `/api/async/image_nanoBanana2` | 0.1元/张 |
| 图片 | GPT-Image-2 | `/api/async/image_gpt` | 0.1元/张 |
| 图片 | NanoBanana Pro | `/api/async/image_nanoBanana_pro` | 0.3元/张 |
| 图片 | NanoBanana-2-Lite | `/api/async/image_nanoBanana2Lite` | 0.08元/张 |
| 视频 | Wan3.0 | `/api/async/video_wan_3.0` | 0.3元/秒 |
| 视频 | MiniMax H3 | `/api/async/video_minimax_h3` | 0.3元/秒 |
| 视频 | veo3.1_fast | `/api/async/video_veo3.1_fast` | 0.05元/秒 |
| 视频 | Digital_Humans | `/api/async/video_digital_humans` | 0.02元/秒 |
| 视频 | Package_1.0 | `/api/async/video_package` | 0.02元/秒 |
| 音频 | 语音合成(异步) | `/api/async/audio_tts` | 0.0006元/字符 |
| 音频 | 语音合成(同步) | `/api/voice/composite` | 0.0006元/字符 |
| 音频 | 语音克隆 | `/api/voice/clone` | 6.0元/次 |
| 查询 | 结果详情 | `/api/async/detail` | 免费 |

---

## 九、功能说明

### 已上线功能（V61）
- ✅ 图片/视频/音频生成
- ✅ 参考图上传（R2）
- ✅ 图片标注编辑器（ImageMarker，悬浮面板式）
- ✅ 缩略图自动生成（320px WebP）
- ✅ 任务中心（挂机生成、Key托管）—— 需配置
- ✅ 批量队列（优先级、排队）—— 需任务中心
- ✅ 口播流水线（文本→音频→数字人→成品）—— 需任务中心
- ✅ 工作流导入/导出
- ✅ 历史记录
- ✅ 明暗主题切换

### 任务中心模式 vs 本地直连模式

| 特性 | 任务中心模式 | 本地直连模式 |
|---|---|---|
| Key 安全 | ✅ 托管在Worker | ❌ 存在前端 |
| 关页面后任务 | ✅ 继续运行 | ❌ 丢失 |
| 批量队列 | ✅ 支持 | ❌ 不支持 |
| 流水线 | ✅ 支持 | ❌ 不支持 |
| 配置难度 | 需Worker | 零配置 |
| 适合场景 | 重度使用 | 轻度/试用 |

---

## 十、故障排查

### 上传图片失败
1. 检查 R2 配置是否正确
2. 检查网络是否能访问 `https://ai-media-proxy.yxiu23.workers.dev`
3. workers.dev 在大陆可能不可达 → 绑定自定义域名

### 任务中心连不上
1. 检查任务中心 URL 和 Token
2. 点「测试」按钮
3. 连不上会自动回退本地直连模式（不影响基础功能）

### 语音/视频无法播放
1. 检查结果 URL 是否 https（http 会被浏览器拦截）
2. 代码已自动把 aliyuncs 的 http 转 https

### 生成超时
1. 语音任务可能需要 15-20 分钟
2. 轮询超时已设为 30 分钟
3. 任务中心模式下关页面不丢任务

---

## 十一、后续更新方法

### 更新前端（我帮你做）
```bash
# 我会执行：
cp 新版本.html /tmp/ai-workbench-dev/ai-workbench-dev.html
cp 新版本移动端.html /tmp/ai-workbench-dev/ai-workbench-mobile-dev.html
cd /tmp/ai-workbench-dev && git add -A && git commit -m "deploy: VXX" && git push
```
1-2 分钟后 GitHub Pages 自动生效。

### 更新 Worker 后端（我帮你做）
通过 Cloudflare API 重新部署 `worker.js`。

---

## 十二、安全提醒

1. **API Key 不要公开分享** — 虽然任务中心模式下前端不存 Key，但本地直连模式仍需要
2. **Auth Token 不要公开分享** — 用于 R2 和任务中心鉴权
3. **Cloudflare API Token 已可删除** — 部署完成后不再需要
4. **定期检查用量** — Cloudflare 免费额度：10万请求/天，KV 10万读/天，1万写/天

---

## 十三、联系方式

如有问题，把错误截图和信息发给我，我来排查修复。

**版本**：V61 全功能版
**部署日期**：2026-09-05
